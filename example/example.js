(function(panel) {
jQuery.extend(panel, {
  example_func2: function(config) {
    //Функция example_func2 вызывается на каждой странице
    // и не может быть отключена или настроена пользователем');
  }
});
})(window.__panel);